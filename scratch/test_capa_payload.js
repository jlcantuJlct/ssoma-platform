const fs = require('fs');
const { Client } = require('pg');

async function run() {
    // Just a script to see what happens when we export Capacitaciones
    console.log("We need to capture the payload of the next export request.");
}
run().catch(console.error);
