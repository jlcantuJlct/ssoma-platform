require('dotenv').config({path: '.env.local'}); const { Client } = require('pg'); const client = new Client({ connectionString: process.env.POSTGRES_URL_NON_POOLING }); client.connect().then(() => client.query(SELECT fields FROM report_drafts WHERE doc_type='PAD_CHINCHAYSULLO_INTERNAL.docx')).then(r => { console.log(JSON.stringify(r.rows[0].fields).substring(0, 500)); client.end() }).catch(console.error);
